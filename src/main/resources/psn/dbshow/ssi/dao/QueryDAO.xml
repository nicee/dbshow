<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="psn.dbshow.ssi.dao.QueryDAO">

    <select id="query" resultType="java.util.HashMap"  statementType="STATEMENT">
        <![CDATA[ SELECT ${columns} FROM ${tableName} LIMIT ${start}, ${size} ]]>
    </select>

</mapper>
